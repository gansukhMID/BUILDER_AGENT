---
name: AbstractUserProfile + AbstractMembership
status: open
created: 2026-05-10T05:33:51Z
updated: 2026-05-10T05:56:21Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/15
depends_on: [13]
parallel: true
conflicts_with: []
---

# Task: AbstractUserProfile + AbstractMembership

## Description

Implement `AbstractUserProfile` and `AbstractMembership` abstract SQLAlchemy models.

Files to create:
- `TEMPLATE_CORE/USER/user_core/models/user_profile.py`
- `TEMPLATE_CORE/USER/user_core/models/membership.py`

## Acceptance Criteria
- [ ] `AbstractUserProfile.__abstract__ == True`
- [ ] Fields: `user_id: Mapped[int]` (bare int, no FK in abstract), `bio: Mapped[str | None]`, `avatar_url: Mapped[str | None]`, `preferences: Mapped[dict]` (JSON column, default `{}`), `timezone: Mapped[str]` (default "UTC"), `locale: Mapped[str]` (default "en")
- [ ] Inherits: `PrimaryKeyMixin`, `TimestampMixin`
- [ ] `AbstractMembership.__abstract__ == True`
- [ ] Fields: `user_id: Mapped[int]` (bare int, no FK in abstract), `tier: Mapped[MembershipTier]` (enum, default `MembershipTier.free`), `started_at: Mapped[datetime]` (server_default=func.now()), `expires_at: Mapped[datetime | None]` (None = non-expiring), `is_active: Mapped[bool]` (default True)
- [ ] Inherits: `PrimaryKeyMixin`, `TimestampMixin`
- [ ] `is_expired` Python property: `return self.expires_at is not None and self.expires_at < datetime.utcnow()`
- [ ] No cross-model Python imports (AD-3)

## Technical Details

`preferences` JSON column:
```python
from sqlalchemy import JSON
preferences: Mapped[dict] = mapped_column(JSON, default=dict)
```

`MembershipTier` imported from `user_core.enums` — no local enum definitions.

## Dependencies

Task 001 (db.py, mixins.py, enums.py)

## Effort Estimate
- Size: S
- Hours: 0.5

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
