---
name: "Middleware update + npm run build passing (integration gate)"
status: closed
created: 2026-05-11T05:07:07Z
updated: 2026-05-11T06:59:52Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/52
depends_on: [49, 50, 51]
parallel: false
conflicts_with: []
---

# Task: Middleware update + npm run build passing

## Description

Final integration gate: extend Next.js middleware to protect store routes, verify `npm run build` passes with no TypeScript errors, and confirm all existing backend tests still pass.

Files to edit:
- `APPS/odoo-frontend/frontend/middleware.ts`

## Acceptance Criteria
- [ ] `middleware.ts` redirects unauthenticated requests to `/store/checkout` → `/store/login?next=/store/checkout`
- [ ] `middleware.ts` redirects unauthenticated requests to `/store/orders/*` → `/store/login?next=<original>`
- [ ] Existing admin route protections (`/dashboard/*`, etc.) still work
- [ ] `npm run build` exits 0 with no TypeScript errors and no missing module errors
- [ ] `pytest` in the backend still passes (≥80% coverage)
- [ ] Manual smoke test: visit `/store`, browse products, add to cart, checkout as logged-in user, see order in history

## Technical Details

Read the current `middleware.ts` first to understand its existing logic. Add the store protected paths alongside existing ones. The access_token check logic is the same.

Current middleware path: `APPS/odoo-frontend/frontend/middleware.ts`

Backend test command (from backend dir with venv active):
```bash
cd APPS/odoo-frontend/backend && source .venv/bin/activate && pytest --cov=. --cov-report=term-missing -q
```

Frontend build command:
```bash
cd APPS/odoo-frontend/frontend && npm run build
```

## Dependencies

- Task 004 (product pages)
- Task 005 (cart + checkout pages)
- Task 006 (order history pages)

## Effort Estimate
- Size: S
- Hours: 2

## Definition of Done
- [ ] Middleware updated
- [ ] `npm run build` passes
- [ ] `pytest` passes
- [ ] End-to-end smoke test completed
