---
name: Project Scaffold
status: closed
created: 2026-05-11T01:36:59Z
updated: 2026-05-11T02:10:00Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/35
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Project Scaffold

## Description

Create the full directory structure for `APPS/odoo-frontend/backend/` and `APPS/odoo-frontend/frontend/`. Set up pyproject.toml for backend (fastapi, uvicorn, python-jose, passlib[bcrypt], pydantic>=2, alembic, sqlalchemy>=2, pytest, pytest-cov, httpx). Set up package.json + tsconfig.json + tailwind.config.ts for Next.js 15 frontend. Create `.env.example` with DATABASE_URL, SECRET_KEY, NEXT_PUBLIC_API_URL.

## Acceptance Criteria
- [ ] `APPS/odoo-frontend/backend/pyproject.toml` exists with all deps
- [ ] `APPS/odoo-frontend/frontend/package.json` exists with next@15, react@19, tailwindcss@4, typescript@5
- [ ] `APPS/odoo-frontend/.env.example` has DATABASE_URL, SECRET_KEY, NEXT_PUBLIC_API_URL
- [ ] `pip install -e .` succeeds in backend dir
- [ ] `npm install` succeeds in frontend dir

## Technical Details

Follow pattern from TEMPLATE_CORE/*/pyproject.toml. For Next.js use `npx create-next-app` pattern with App Router, TypeScript, Tailwind. No test files yet — just scaffold.

## Dependencies

None — this is the foundation for all subsequent tasks.

## Effort Estimate
- Size: S
- Hours: 1

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
