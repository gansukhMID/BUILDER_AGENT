---
name: Concrete Models + Alembic Migration
status: closed
created: 2026-05-11T01:36:59Z
updated: 2026-05-11T01:58:46Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/36
depends_on: [35]
parallel: false
conflicts_with: []
---

# Task: Concrete Models + Alembic Migration

## Description

In `APPS/odoo-frontend/backend/`, create `models/concrete.py` that subclasses all 9 AbstractUser models from user_core (following the pattern in TEMPLATE_CORE/USER/tests/concrete_models.py), and imports WMS + ECOMMERCE concrete models directly. Create `db.py` with SQLAlchemy 2.x engine + session factory. Create Alembic config + `alembic/versions/0001_initial.py` migration that creates all tables.

## Acceptance Criteria
- [ ] `models/concrete.py` defines concrete User, OAuthAccount, UserProfile, Membership, Order, OrderLine, Purchase, Review, ActivityEvent (from user_core abstract)
- [ ] `db.py` exposes `engine`, `SessionLocal`, `get_db()` dependency
- [ ] `alembic upgrade head` creates all tables in SQLite without errors
- [ ] All 35+ tables present after migration (9 user + 10 WMS + 16 ecommerce)

## Technical Details

Install templates editable: `pip install -e ../../TEMPLATE_CORE/USER -e ../../TEMPLATE_CORE/WMS -e ../../TEMPLATE_CORE/ECOMMERCE`. Import Base from user_core.db for user models; import wms_core.models and ecommerce_core.models. Alembic env.py must include all three Bases.

## Dependencies

- 001 (Project Scaffold) — backend pyproject.toml and directory structure must exist first.

## Effort Estimate
- Size: M
- Hours: 2

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
