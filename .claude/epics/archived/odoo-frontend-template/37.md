---
name: FastAPI Foundation
status: closed
created: 2026-05-11T01:36:59Z
updated: 2026-05-11T01:58:46Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/37
depends_on: [36]
parallel: false
conflicts_with: []
---

# Task: FastAPI Foundation

## Description

Create `main.py` FastAPI app with CORS middleware (allow all origins for dev), include all four routers (auth, wms, ecommerce, users — stubs OK at this stage), add health check `GET /health` returning `{"status": "ok"}`, configure Swagger UI title "Odoo Frontend API". Create `backend/db.py` (if not already in 002).

## Acceptance Criteria
- [ ] `uvicorn main:app --reload` starts without errors
- [ ] `GET /health` returns `{"status": "ok"}` with 200
- [ ] Swagger UI loads at `http://localhost:8000/docs` showing all route groups
- [ ] CORS headers present on responses

## Technical Details

Use `FastAPI(title="Odoo Frontend API", version="1.0.0")`. Routers can be empty at this point — just `APIRouter()` with a placeholder. Main structure: `app.include_router(auth_router, prefix="/auth", tags=["auth"])` etc.

## Dependencies

- 002 (Concrete Models + Alembic Migration) — db.py and models must be in place before wiring routers.

## Effort Estimate
- Size: S
- Hours: 1

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
