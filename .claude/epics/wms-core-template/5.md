---
name: Lot Model
status: closed
created: 2026-05-08T06:08:06Z
updated: 2026-05-09T08:05:49Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/5
depends_on: [2]
parallel: false
conflicts_with: []
---

# Task: Lot Model

## Description

Implement the `Lot` model — lot and serial number tracking tied to a product.

Files to create:
- TEMPLATE_CORE/WMS/wms_core/models/lot.py

Lot model fields:
- `id: Mapped[int]` (primary key)
- `product_id: Mapped[int]` (FK to Product, non-nullable)
- `ref: Mapped[Optional[str]]` (internal reference, nullable)
- `expiration_date: Mapped[Optional[datetime]]` (nullable)
- Inherits: `TimestampMixin`, `ActiveMixin`, `NameMixin`
- Relationship: `product: Mapped["Product"]` (back_populates="lots")
- Unique constraint: `(name, product_id)` — same lot name can exist for different products

## Acceptance Criteria
- [ ] `from wms_core.models.lot import Lot` succeeds
- [ ] `Lot.__tablename__` = `"stock_lot"`
- [ ] FK to Product enforced
- [ ] Unique constraint on `(name, product_id)` — verified by attempting duplicate insert
- [ ] `product` relationship loads the related `Product`
- [ ] Add `lots` backref on `Product` (update product.py or use `back_populates`)

## Technical Details

Add `lots: Mapped[list["Lot"]] = relationship(back_populates="product")` to Product. If modifying product.py, use a lazy import or string reference to avoid circular imports. `name` comes from `NameMixin`.

## Dependencies

Task 005 (Product)

## Effort Estimate
- Size: XS
- Hours: 1

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
