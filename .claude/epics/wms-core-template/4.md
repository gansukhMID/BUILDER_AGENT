---
name: Project Scaffold + db.py
status: open
created: 2026-05-08T06:08:06Z
updated: 2026-05-08T06:27:40Z
github: https://github.com/gansukhMID/BUILDER_AGENT/issues/4
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Project Scaffold + db.py

## Description

Bootstrap the wms_core Python package and implement db.py — the shared SQLAlchemy foundation imported by all models.

Files to create:
- TEMPLATE_CORE/WMS/pyproject.toml  (deps: sqlalchemy>=2.0, alembic>=1.13, python-dotenv>=1.0; dev: pytest, pytest-cov)
- TEMPLATE_CORE/WMS/wms_core/__init__.py  (empty or version string)
- TEMPLATE_CORE/WMS/wms_core/db.py  (Base, engine_factory, SessionFactory, get_session context manager)
- TEMPLATE_CORE/WMS/wms_core/models/__init__.py  (empty placeholder)
- TEMPLATE_CORE/WMS/wms_core/utils/__init__.py  (empty)
- TEMPLATE_CORE/WMS/tests/__init__.py  (empty)
- TEMPLATE_CORE/WMS/tests/conftest.py  (SQLite in-memory engine + session fixtures)

db.py must export:
- `Base` — SQLAlchemy 2.x `DeclarativeBase` subclass
- `engine_factory(dsn: str) -> Engine`
- `SessionFactory(engine) -> sessionmaker`
- `get_session(engine)` — context manager yielding a `Session`

## Acceptance Criteria
- [ ] `from wms_core.db import Base, engine_factory, get_session` succeeds
- [ ] `Base.metadata` has no tables yet (empty at this stage)
- [ ] `engine_factory("sqlite:///:memory:")` returns a working Engine
- [ ] `get_session(engine)` context manager commits on success, rolls back on exception
- [ ] `pyproject.toml` installs with `pip install -e .`
- [ ] `conftest.py` provides `engine` and `session` fixtures for all tests

## Technical Details

Use SQLAlchemy 2.x style: `class Base(DeclarativeBase): pass`. engine_factory uses `create_engine(dsn, echo=False)`. SessionFactory uses `sessionmaker(bind=engine, expire_on_commit=False)`. get_session is a `@contextmanager` that yields session and handles rollback.

## Dependencies

None

## Effort Estimate
- Size: S
- Hours: 1-2

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
